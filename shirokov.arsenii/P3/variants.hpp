#ifndef VARIANTS_HPP
#define VARIANTS_HPP
#include <cstddef>

namespace shirokov
{
  void spiral(int *matrix, size_t m, size_t n);
  bool isTriangularMatrix(const int *matrix, size_t m, size_t n);
}

#endif
